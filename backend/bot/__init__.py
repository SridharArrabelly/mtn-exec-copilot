"""Teams conversational bot (issue #53, Phase 2a).

Hosts a Microsoft 365 Agents SDK bot inside the existing FastAPI app and
bridges Teams message turns to the existing Foundry agent (reused unchanged).
"""
