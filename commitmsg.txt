docs: drop GitHub Actions CI/CD path (manual azd deploy only)

Remove the optional automated-deploy story now that deploys are done
manually with `azd up`:

- Delete .github/workflows/azure-dev.yml (OIDC azd provision/deploy)
- Remove the "BYO with GitHub Actions" README subsection
- Remove the "CI/CD with GitHub Actions" README section

Nothing else in the project depended on the workflow.

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
